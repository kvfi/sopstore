apiVersion: v1
kind: Service
metadata:
  name: {{ include "sopstore.fullname" . }}
  labels: {{- include "sopstore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{- include "sopstore.selectorLabels" . | nindent 4 }}
