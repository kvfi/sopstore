# Gradle 9.5.1 runs fine on JDK 25, so the daemon uses the JDK that launches the
# wrapper (JDK 25 on PATH). This hint just ensures toolchain resolution can locate
# the JDK 25 install for `java { toolchain { languageVersion = 25 } }`.
org.gradle.java.installations.paths=/usr/lib/jvm/jdk-25.0.3+9
